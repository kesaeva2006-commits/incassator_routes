# Лёгкие зависимости для автосборки в GitHub Actions.
# Без osmnx и геобиблиотек — сборщик использует быстрый режим (use_graph=False),
# поэтому граф дорог Москвы не нужен. Это ускоряет CI и делает его надёжным.
folium>=0.14.0
numpy>=1.21.0
scikit-learn>=1.0.0
networkx>=2.6.0
requests>=2.25.0
